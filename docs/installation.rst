============
Installation
============

At the command line::

    $ easy_install kicost

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv kicost
    $ pip install kicost
